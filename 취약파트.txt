취약파트

boolean은 true false 이외에는 오류 발생
char 숫자로 저장된다 우리는 숫자 대신에 
홑따옴표 사용 ('a', 'A', '가',...)
형변환
double d1 = 35.14;
int d2 =(int) d1;
++(--)변수 :
현재변수가 가지고 있는값에 1을증가(감소)하고 
나머지를 실행
변수 ++(--) : 
현재 변수를가지고 실행을 하고 증감진행
!=(=다르다, 같지않다)
패키지가 java.lang에 존재하는 클래스를 사용할때
는 import를 할 필요가 없다.
java.lang이외에 존재하는 클래스를 사용하기 위해서
import를 해야된다.
Scanner scan = new Scanner(System.in);
System.out.print("이름 : ");
String name = scan.next();
2299원 이럴때 100원 아래로 버릴때 나누기100곱100 int형변환
char 선언할때 '기억
char k = 'd'; 따옴표 확인
System.out.print("몇 잔?");
int su = scan.nextInt();   
System.out.print("입금할 금액");
double input = scan.nextDouble();
포문은 끝을 맞으면 증감식으로 가고
와일문은 끝을 맞으면 조건식으로 감
횟수가 확실하면 포문
횟수가 애매하면 와일문
